// app/products/_layout.tsx
// Stack du module produits (création, détail, édition).
// Les titres sont définis par chaque écran via <Stack.Screen options>.
import { Stack } from 'expo-router';

export default function ProductsStackLayout() {
  return <Stack />;
}
