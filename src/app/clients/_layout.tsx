// app/clients/_layout.tsx
// Layout Stack pour les écrans clients hors onglets (création, détail, édition).
// Les titres précis sont définis par chaque écran via <Stack.Screen options>.

import { Stack } from 'expo-router';

export default function ClientsStackLayout() {
  return <Stack />;
}
